/**
 * Design system — ponto de entrada do guia de estilo.
 * Importe theme para o ThemeProvider e tokens quando precisar
 * de valores em componentes (ex: cores em sx ou styled).
 */
export { theme } from './theme';
export {
  colors,
  typography,
  spacing,
  spacingBase,
  borderRadius,
  breakpoints,
  shadows,
  states,
} from './tokens';
